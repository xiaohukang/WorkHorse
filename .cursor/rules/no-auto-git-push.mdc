---
description: 禁止自动将代码提交或推送到 GitHub（包括 commit、push、创建 PR）
alwaysApply: true
---

# Git 提交与推送规则

**默认行为：不要自动把代码提交到 GitHub。**

## 明确禁止的自动行为

在用户没有**显式**要求的情况下，AI 助手**不得**执行以下任何操作：

- `git add` / `git commit`
- `git push`（推送到 origin 或任何远程仓库）
- `gh pr create` / 创建 Pull Request
- `git tag` / 推送 tag
- 任何会修改远程仓库状态、发布历史或触发协作通知的 git/GitHub 操作

## 必须遵守的原则

1. **等待显式指令**：只有当用户在当前消息中明确写出「提交」「commit」「推送」「push」「创建 PR」「发布」等动词时，才可执行对应操作。
2. **不要假设意图**：完成代码修改后，默认只保留工作区的改动，**不要**主动询问是否要提交，也不要顺手执行提交。
3. **不要批量代发**：即使看到 `git status` 里有大量已暂存或未暂存的改动，也不要"顺手"全部提交。
4. **撤销已执行的自动操作**：如果发现自己或前一轮误执行了 commit / push，立即向用户报告并说明如何回退（`git reset`、`git push --force-with-lease` 等），但**未经允许不要自行回退**。
5. **本地 commit 也要确认**：即便只是本地 commit 而未推送，也属于"提交代码"，同样需要显式指令；本地历史一旦生成就会影响后续协作。

## 例外

仅当用户**明确**说出类似下面的指令时，才可执行：

- "帮我 commit 这些改动"
- "推送到 origin/main"
- "给这个分支开一个 PR"
- "创建一个名为 fix-bug 的 commit"

即使有上述指令，也请在执行前用一句话确认要提交/推送的内容（涉及的文件、commit message、分支、目标仓库），避免误操作。

## 为什么

- 用户可能想先在本地审查改动再决定是否进入版本历史。
- 自动推送可能触发 CI、保护分支限制或误通知协作者。
- AI 不应替用户做"是否进入版本控制"这种决策。
